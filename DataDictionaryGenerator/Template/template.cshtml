@using System.Linq
@using DataDictionaryGenerator.Model
@model System.Collections.Generic.Dictionary<string, System.Collections.Generic.List<TableColumnInfo>>
@if (Model.Any())
{
foreach (string tableName in Model.Keys)
{
<text>## @tableName</text>
<text>> @tableName</text>
@:| **字段名** | **主键** | **类型** | **允许空** | **字段说明** | 
@:| --- | --- | --- | --- | --- |
    if (Model[tableName].Any())
    {
    foreach (TableColumnInfo columnInfo in Model[tableName])
    {
<text>| @columnInfo.Name | @(columnInfo.IsPrimary) | @columnInfo.Type | @columnInfo.AllowNull | @columnInfo.Description | </text>
    }
    }
    }
    }